# apps/docs vs the shipped API — mechanical alignment report

Generated by `apps/docs/scripts/docs-alignment/` at commit `4ee12d5`, against `@kitn.ai/ui` 0.19.0 as built into `packages/ui/dist`.

**This is a snapshot.** Every name checked here is read at run time from the built `dist/*.d.ts` and from `src/elements/element-meta.json` — nothing is baked into the harness — so the numbers move when the API moves. Re-run rather than trusting the counts below once the surface changes.

## How this was found

Doc snippets are fragments: they call the reader's own functions, omit imports, and import packages this repo does not install. Compiling them naively produces a wall of false positives and the check gets switched off. The line this harness holds instead:

> A snippet referencing the READER's code must not fail. A snippet that imports or uses a **kit** symbol that does not exist, or passes a prop a kit component does not accept, must fail.

Four mechanisms do that:

1. **Unknowns are resolved by the compiler, not guessed.** Compile, collect every `Cannot find name 'X'`, declare those as `any`, recompile until it settles. The compiler's own scope analysis decides what is undeclared. Stubs sit on one prepended line inside a module, so they are scoped to their own snippet and every reported line number is off by exactly one, which is subtracted back out. When the unknown name IS a kit export and the snippet already imports from the kit, the real import is injected instead of an `any` stub, so a fragment that omits an import still gets its props checked.
2. **Kit modules are never shimmed.** Third-party specifiers that do not resolve become shorthand ambient modules (`any`). `@kitn.ai/ui` and its subpaths are excluded by a hard guard — an unresolved kit entry point or a missing exported member always fails.
3. **Findings are split by ORIGIN, not by error code.** For each diagnostic the checker is asked where the offending type was *declared*; anything declared under `packages/ui` is kit drift. That is what separates "unknown prop on a kit component" from "unknown prop on the reader's own object", and it lets untyped `js` blocks be checked for kit drift only.
4. **Fragments are interpreted, not rejected.** A block that does not parse as a module is retried as a type shape, a class member, and a bare value; the first interpretation that parses is used. Genuinely malformed code still fails, which the self-test proves.

The `kai-*` contract is checked **structurally** against `element-meta.json` rather than by tsc, because the kit's shipped React JSX augmentation types every element as `{ [attr: string]: unknown }` and cannot reject a misspelled attribute — and because 132 of the 346 blocks are plain `html` that tsc never sees.

Two deliberate exemptions, both self-tested so they cannot silently swallow real findings: snippets the docs explicitly mark as counter-examples (`<!-- Fails — an array can't be an HTML attribute -->`), and JSX placeholder children (`{/* your components */}`).

## Headline

| | |
|---|---|
| Pages scanned | 120 |
| Fenced blocks | 346 |
| Snippets compiled | 255 |
| Entry points read | 7 |
| Exported names read | 1079 |
| `kai-*` elements read | 79 |
| **High-confidence findings** | **11** |
| Medium findings | 7 |
| Advisory findings | 64 |
| Flagged for human review | 47 |

### By finding kind

| count | kind | what it means |
|---:|---|---|
| 29 | `snippet-type-error` | a type error in a snippet not traceable to a kit type (advisory) |
| 18 | `undeclared-in-element-meta` | the kit HAS this (a declarative light-DOM child, or an event it dispatches) but `element-meta.json` does not declare it, so the generated types, docs tables and MCP catalog all omit it — a metadata gap, not a docs error |
| 10 | `unresolved-prose-name` | prose backticks a PascalCase name no entry point exports and the page never defines (advisory) |
| 9 | `literal-widening` | a plain object literal widens (`role: 'user'` → `string`) and no longer fits the kit's prop type — a TS reader who copies this does not compile; the fix is the kit's exported `ChatMessage` |
| 7 | `redeclared-kit-type` | a snippet re-declares a type the kit already exports (compiles, but teaches a private copy) |
| 6 | `kit-type-error` | a snippet uses a kit symbol/prop/signature that does not typecheck against the shipped `.d.ts` |
| 2 | `stale-kit-api-name` | prose names something that reads as kit API (it extends a real export family) but that no entry point exports — a consumer cannot import it |
| 1 | `incompatible-local-type` | a snippet re-declares a kit type narrower than the real one, and its code breaks against the real one |

### By docs section, ranked by consumer impact

| section | pages | high | medium | advisory |
|---|---:|---:|---:|---:|
| `guides/frameworks` | 8 | 6 | 0 | 15 |
| `guides` | 15 | 3 | 4 | 10 |
| `components` | 59 | 2 | 0 | 22 |
| `examples` | 14 | 0 | 0 | 3 |
| `guides/recipes` | 4 | 0 | 2 | 0 |
| `(root)` | 1 | 0 | 0 | 0 |
| `integrations` | 9 | 0 | 0 | 12 |
| `patterns` | 9 | 0 | 1 | 2 |
| `theme` | 1 | 0 | 0 | 0 |

## Fix first

The 11 high-confidence findings collapse into 6 root cause(s). Each is one edit repeated across pages.

| root cause | pages affected |
|---|---|
| `PromptInputActionsProps` requires `children`, which the snippet does not pass | `guides/getting-started.mdx`:217, `guides/frameworks/overview.mdx`:178, `guides/frameworks/solid.mdx`:100 |
| an inline message array widens (`role: 'user'` → `string`) and no longer fits the kit's message type | `guides/getting-started.mdx`:141, `guides/use-the-chat-app.mdx`:168 |
| `ConversationsProps` requires `groups`, which the snippet does not pass | `guides/frameworks/react.mdx`:108, `guides/frameworks/react.mdx`:137 |
| stale-kit-api-name | `components/message.mdx`:29, `components/prompt-input.mdx`:61 |
| a snippet re-declares a kit type incompatibly | `guides/frameworks/solid.mdx`:40 |
| `ConversationListProps` requires `groups`, which the snippet does not pass | `guides/frameworks/solid.mdx`:125 |

## High-confidence findings (11)

### `guides/getting-started.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 141 | `literal-widening` TS2322 | Type '{ id: string; role: string; parts: { type: string; text: string; }[]; }[]' is not assignable to type '{ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string; raw?: { source: string; payload: unknown; } \| undefined; } \| { type: "reasoning"; text: string; label?: string \|… |
| 217 | `kit-type-error` TS2322 | Type '{}' is not assignable to type 'IntrinsicAttributes & PromptInputActionsProps'. Property 'children' is missing in type '{}' but required in type 'PromptInputActionsProps'. |

### `guides/frameworks/overview.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 178 | `kit-type-error` TS2322 | Type '{}' is not assignable to type 'IntrinsicAttributes & PromptInputActionsProps'. Property 'children' is missing in type '{}' but required in type 'PromptInputActionsProps'. |

### `guides/frameworks/react.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 108 | `kit-type-error` TS2741 | Property 'groups' is missing in type '{ conversations: any; activeId: any; onConversationSelect: (e: CustomEvent<{ id: string; }>) => void; onNewChat: () => any; style: { width: number; flexShrink: number; }; }' but required in type 'ConversationsProps'. |
| 137 | `kit-type-error` TS2741 | Property 'groups' is missing in type '{ conversations: any; activeId: any; onConversationSelect: any; }' but required in type 'ConversationsProps'. |

### `guides/use-the-chat-app.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 168 | `literal-widening` TS2322 | Type '{ id: string; role: string; parts: { type: string; text: string; }[]; }[]' is not assignable to type '{ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string; raw?: { source: string; payload: unknown; } \| undefined; } \| { type: "reasoning"; text: string; label?: string \|… |

### `guides/frameworks/solid.mdx` — 3 finding(s)

| line | kind | detail |
|---:|---|---|
| 40 | `incompatible-local-type` | snippet re-declares `ChatMessage` — a type @kitn.ai/ui already exports — and is NOT compatible with the real one: TS2339 Property 'text' does not exist on type 'MessagePart'. Property 'text' does not exist on type '{ type: "tool"; tool: ToolPart; raw?: RawOrigin \| undefined; }'. |
| 100 | `kit-type-error` TS2322 | Type '{}' is not assignable to type 'IntrinsicAttributes & PromptInputActionsProps'. Property 'children' is missing in type '{}' but required in type 'PromptInputActionsProps'. |
| 125 | `kit-type-error` TS2322 | Type '{ conversations: any; activeId: string \| undefined; onSelect: (id: string) => string; onNewChat: () => any; }' is not assignable to type 'IntrinsicAttributes & ConversationListProps'. Property 'groups' is missing in type '{ conversations: any; activeId: string \| undefined; onSelect: (id: strin… |

### `components/message.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 29 | `stale-kit-api-name` | `MessageActionBar` reads as kit API (the kit exports `Message`) but no entry point exports it — a consumer cannot import this |

### `components/prompt-input.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 61 | `stale-kit-api-name` | `ComposerDoc` reads as kit API (the kit exports `Composer`) but no entry point exports it — a consumer cannot import this |

## Medium findings (7)

### `guides/compose-message-thread.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 46 | `literal-widening` TS2322 | Type '{ id: string; role: string; parts: { type: string; text: string; }[]; actions?: undefined; } \| { id: string; role: string; parts: { type: string; text: string; }[]; actions: string[]; }' is not assignable to type '{ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string;… |

### `guides/for-ai-agents.mdx` — 3 finding(s)

| line | kind | detail |
|---:|---|---|
| 214 | `literal-widening` TS2322 | Type '({ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string; raw?: { source: string; payload: unknown; }; } \| { type: "reasoning"; text: string; label?: string; index?: number; streamId?: string; signature?: string; raw?: { ...; }; } \| { ...; } \| { ...; } \| { ...; } \| { ...… |
| 219 | `literal-widening` TS2322 | Type 'string' is not assignable to type '"user" \| "assistant"'. |
| 219 | `literal-widening` TS2322 | Type 'undefined[] \| ({ type: "text"; text: string; raw?: { source: string; payload: unknown; }; } \| { type: "reasoning"; text: string; label?: string; index?: number; streamId?: string; signature?: string; raw?: { source: string; payload: unknown; }; } \| { ...; } \| { ...; } \| { ...; } \| { ...; })[]… |

### `guides/recipes/streaming.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 31 | `literal-widening` TS2322 | Type '({ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string; raw?: { source: string; payload: unknown; }; } \| { type: "reasoning"; text: string; label?: string; index?: number; streamId?: string; signature?: string; raw?: { ...; }; } \| { ...; } \| { ...; } \| { ...; } \| { ...… |
| 47 | `literal-widening` TS2345 | Argument of type '({ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string; raw?: { source: string; payload: unknown; }; } \| { type: "reasoning"; text: string; label?: string; index?: number; streamId?: string; signature?: string; raw?: { ...; }; } \| { ...; } \| { ...; } \| { ..… |

### `patterns/compose-your-own.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 59 | `literal-widening` TS2322 | Type '{ id: string; role: string; parts: { type: string; text: string; }[]; actions?: undefined; } \| { id: string; role: string; parts: { type: string; text: string; }[]; actions: string[]; }' is not assignable to type '{ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string;… |

## Advisory findings (64)

Not gating. Type errors that do not trace back to a kit type, prose names the harness could not resolve, and snippets that re-declare a kit type without actually breaking against it.

### `guides/frameworks/react.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 41 | `redeclared-kit-type` | snippet re-declares `MessagePart`, which @kitn.ai/ui already exports (the local copy compiles, but readers learn a type the kit owns) |
| 156 | `snippet-type-error` TS2657 | JSX expressions must have one parent element. |

### `guides/generative-ui.mdx` — 3 finding(s)

| line | kind | detail |
|---:|---|---|
| 28 | `redeclared-kit-type` | snippet re-declares `CardEnvelope`, which @kitn.ai/ui already exports (the local copy compiles, but readers learn a type the kit owns) |
| 83 | `snippet-type-error` TS7006 | Parameter 'call' implicitly has an 'any' type. |
| 144 | `snippet-type-error` TS7006 | Parameter 'e' implicitly has an 'any' type. |

### `guides/state-and-hooks.mdx` — 6 finding(s)

| line | kind | detail |
|---:|---|---|
| 40 | `snippet-type-error` TS7006 | Parameter 'm' implicitly has an 'any' type. |
| 43 | `snippet-type-error` TS7006 | Parameter 'm' implicitly has an 'any' type. |
| 46 | `snippet-type-error` TS7006 | Parameter 'm' implicitly has an 'any' type. |
| 49 | `snippet-type-error` TS7006 | Parameter 'm' implicitly has an 'any' type. |
| 147 | `snippet-type-error` TS2339 | Property 'kai-chat' does not exist on type 'JSX.IntrinsicElements'. |
| 162 | `snippet-type-error` TS2657 | JSX expressions must have one parent element. |

### `guides/theming.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 167 | `snippet-type-error` TS2322 | Type '{ proseSize: "base"; codeTheme: string; }' is not assignable to type 'IntrinsicAttributes & ChatConfigProps'. Property 'children' is missing in type '{ proseSize: "base"; codeTheme: string; }' but required in type 'ChatConfigProps'. |

### `guides/frameworks/angular.mdx` — 3 finding(s)

| line | kind | detail |
|---:|---|---|
| 24 | `snippet-type-error` TS2307 | Cannot find module './app/app.component' or its corresponding type declarations. |
| 25 | `snippet-type-error` TS2307 | Cannot find module './app/app.config' or its corresponding type declarations. |
| 54 | `redeclared-kit-type` | snippet re-declares `MessagePart`, which @kitn.ai/ui already exports (the local copy compiles, but readers learn a type the kit owns) |

### `guides/frameworks/meta-frameworks.mdx` — 3 finding(s)

| line | kind | detail |
|---:|---|---|
| 38 | `snippet-type-error` TS2307 | Cannot find module './chat-island' or its corresponding type declarations. |
| 67 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |
| 114 | `snippet-type-error` TS7006 | Parameter 'm' implicitly has an 'any' type. |

### `guides/frameworks/solid.mdx` — 4 finding(s)

| line | kind | detail |
|---:|---|---|
| 134 | `snippet-type-error` TS2322 | Type '{ class: string; }' is not assignable to type 'IntrinsicAttributes & ChatContainerContentProps'. Property 'children' is missing in type '{ class: string; }' but required in type 'ChatContainerContentProps'. |
| 159 | `snippet-type-error` TS2657 | JSX expressions must have one parent element. |
| 173 | `redeclared-kit-type` | snippet re-declares `MessagePart`, which @kitn.ai/ui already exports (the local copy compiles, but readers learn a type the kit owns) |
| 196 | `snippet-type-error` TS2339 | Property 'kai-chat' does not exist on type 'JSX.IntrinsicElements'. |

### `guides/frameworks/vue.mdx` — 3 finding(s)

| line | kind | detail |
|---:|---|---|
| 24 | `snippet-type-error` TS2307 | Cannot find module './App.vue' or its corresponding type declarations. |
| 43 | `snippet-type-error` TS7006 | Parameter 'tag' implicitly has an 'any' type. |
| 78 | `redeclared-kit-type` | snippet re-declares `MessagePart`, which @kitn.ai/ui already exports (the local copy compiles, but readers learn a type the kit owns) |

### `components/badge.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 26 | `unresolved-prose-name` | `Beta` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `components/chain-of-thought.mdx` — 5 finding(s)

| line | kind | detail |
|---:|---|---|
| 46 | `undeclared-in-element-meta` | `kai-step` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |
| 46 | `undeclared-in-element-meta` | `kai-step` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |
| 50 | `undeclared-in-element-meta` | <kai-step> is used by the kit (a declarative light-DOM child) but is not a registered element, so element-meta.json, the generated types and the MCP catalog all omit it |
| 51 | `undeclared-in-element-meta` | <kai-step> is used by the kit (a declarative light-DOM child) but is not a registered element, so element-meta.json, the generated types and the MCP catalog all omit it |
| 52 | `undeclared-in-element-meta` | <kai-step> is used by the kit (a declarative light-DOM child) but is not a registered element, so element-meta.json, the generated types and the MCP catalog all omit it |

### `components/command.mdx` — 4 finding(s)

| line | kind | detail |
|---:|---|---|
| 52 | `unresolved-prose-name` | `Chats` is not exported by any @kitn.ai/ui entry point and is not defined on this page |
| 52 | `unresolved-prose-name` | `Files` is not exported by any @kitn.ai/ui entry point and is not defined on this page |
| 58 | `unresolved-prose-name` | `Actions` is not exported by any @kitn.ai/ui entry point and is not defined on this page |
| 58 | `unresolved-prose-name` | `Settings` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `components/composer.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 60 | `unresolved-prose-name` | `EntityRef` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `components/conversations.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 65 | `undeclared-in-element-meta` | `kai-conversation` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |
| 95 | `undeclared-in-element-meta` | `kai-conversation` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |

### `components/message.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 115 | `unresolved-prose-name` | `Share` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `components/model-switcher.mdx` — 4 finding(s)

| line | kind | detail |
|---:|---|---|
| 59 | `undeclared-in-element-meta` | `kai-model` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |
| 63 | `undeclared-in-element-meta` | <kai-model> is used by the kit (a declarative light-DOM child) but is not a registered element, so element-meta.json, the generated types and the MCP catalog all omit it |
| 64 | `undeclared-in-element-meta` | <kai-model> is used by the kit (a declarative light-DOM child) but is not a registered element, so element-meta.json, the generated types and the MCP catalog all omit it |
| 68 | `undeclared-in-element-meta` | `kai-model` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |

### `components/resizable.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 51 | `undeclared-in-element-meta` | `kai-maximize-intent` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |

### `components/skills.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 16 | `undeclared-in-element-meta` | `kai-skill` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |
| 25 | `undeclared-in-element-meta` | `kai-skill` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |

### `components/suggestions.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 46 | `undeclared-in-element-meta` | `kai-suggestion` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |

### `integrations/cloudflare-ai.mdx` — 5 finding(s)

| line | kind | detail |
|---:|---|---|
| 20 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |
| 24 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |
| 54 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |
| 54 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |
| 58 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |

### `integrations/connect-any-backend.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 15 | `redeclared-kit-type` | snippet re-declares `MessagePart`, which @kitn.ai/ui already exports (the local copy compiles, but readers learn a type the kit owns) |

### `integrations/connect-any-model.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 38 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |

### `integrations/harnesses.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 65 | `snippet-type-error` TS7031 | Binding element 'update' implicitly has an 'any' type. |
| 83 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |

### `integrations/langgraph.mdx` — 3 finding(s)

| line | kind | detail |
|---:|---|---|
| 22 | `snippet-type-error` TS7031 | Binding element 'city' implicitly has an 'any' type. |
| 62 | `unresolved-prose-name` | `StateGraph` is not exported by any @kitn.ai/ui entry point and is not defined on this page |
| 75 | `unresolved-prose-name` | `ToolMessage` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `patterns/custom-chat-header.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 55 | `undeclared-in-element-meta` | `kai-model` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |

### `patterns/popover-menu.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 51 | `unresolved-prose-name` | `Placement` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `examples/model-context.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 108 | `undeclared-in-element-meta` | `kai-model` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |

### `examples/remote-cards.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 171 | `redeclared-kit-type` | snippet re-declares `CardEnvelope`, which @kitn.ai/ui already exports (the local copy compiles, but readers learn a type the kit owns) |

### `examples/skills-assistant.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 91 | `undeclared-in-element-meta` | `kai-skill` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so element-meta.json omits it |

## Reverse coverage

### Shipped but undocumented

**19 `kai-*` element(s) are never mentioned anywhere in `apps/docs`:**

`kai-agent-card`, `kai-coachmark`, `kai-dialog`, `kai-editable-label`, `kai-input`, `kai-kbd`, `kai-nav`, `kai-pane`, `kai-pane-group`, `kai-progress-bar`, `kai-prompt-dock`, `kai-screen`, `kai-segmented`, `kai-setting-item`, `kai-settings-group`, `kai-status`, `kai-tabs`, `kai-thread`, `kai-voice-output`

**1 element(s) are mentioned but have no `<Playground>`/`<Example>`/`<PropTable>` of their own** (named in passing only):

| element | mentioned in |
|---|---|
| `kai-search` | `components/prompt-input.mdx`, `guides/use-the-chat-app.mdx` |

**89 SolidJS component export(s) are never named in the docs** (of 163 detected components on the root entry):

`AgentCard`, `AttachmentEmpty`, `AttachmentHoverCard`, `AttachmentHoverCardContent`, `AttachmentHoverCardTrigger`, `AttachmentInfo`, `AttachmentPreview`, `AttachmentRemove`, `BarsLoader`, `ChainOfThoughtAccordion`, `ChainOfThoughtItem`, `CheckpointIcon`, `CheckpointTrigger`, `ChoiceCard`, `CircularLoader`, `ClassicLoader`, `Coachmark`, `CodeBlockGroup`, `CollapsedRail`, `CollapsibleContent`, `CollapsibleTrigger`, `CommandList`, `ConfirmCard`, `ContextCacheUsage`, `ContextContent`, `ContextContentBody`, `ContextContentFooter`, `ContextContentHeader`, `ContextInputUsage`, `ContextOutputUsage`, `ContextReasoningUsage`, `ContextTrigger`, `Dialog`, `DismissedStub`, `DotsLoader`, `Dropdown`, `DropdownCheckboxItem`, `DropdownContent`, `DropdownItem`, `DropdownLabel`, `DropdownRadioItem`, `DropdownSeparator`, `DropdownSub`, `DropdownSubContent`, `DropdownSubTrigger`, `DropdownTrigger`, `EditableLabel`, `EmptyContent`, `EmptyDescription`, `EmptyHeader`, `EmptyMedia`, `EmptyTitle`, `FileUploadContent`, `HoverCard`, `HoverCardContent`, `HoverCardRoot`, `HoverCardTrigger`, `Kbd`, `MessageAction`, `MessageBody`, `Nav`, `Pane`, `PaneGroup`, `ProgressBar`, `PromptDock`, `PromptSuggestion`, `PulseDotLoader`, `PulseLoader`, `ResizableHandle`, `ResizablePanelGroup`, `ResponseCompare`, `Screen`, `ScrollArea`, `Segmented`, `SettingItem`, `SettingsGroup`, `SourceContent`, `SourceList`, `SourceTrigger`, `TasksCard`, `TerminalLoader`, `TextBlinkLoader`, `TextDotsLoader`, `TextShimmerLoader`, `Textarea`, `ToastRegion`, `TypingLoader`, `VoiceOutput`, `WaveLoader`

### Documented but gone

**0 `kai-*` token(s) appear in the docs but are neither a registered element nor a declared event:**

_none_

**0 `@kitn.ai/ui` subpath(s) referenced in the docs are not in the package's exports map:**

_none_

## Flagged for human review

Executable factual claims the harness extracted but cannot adjudicate. It does **not** guess whether these are true — someone has to check each against the build output, the exports map, or the running app.

### `default` (18) — claims about default values and automatic behaviour

| page | line | claim |
|---|---:|---|
| `guides/installation.mdx` | 79 | Inherited custom properties cross the Shadow DOM boundary, so these reach every `kai-*` element automatically. Prefer shadcn-style unprefixed `--color-*` tokens? Import `@kitn.ai/ui/theme.css` and override those instead — see [Theming](/guides/theming/) for the full token list, dark mode, and typogr |
| `guides/theming.mdx` | 36 | The kit injects its own scoped CSS into each component's Shadow DOM automatically — no import required to get started. Import `theme.css` only to override tokens: |
| `guides/theming.mdx` | 103 | The kit does not automatically apply `.dark` based on `prefers-color-scheme` — that decision belongs to the host app. Add the class in response to the media query if you want OS-level sync: |
| `guides/use-the-chat-app.mdx` | 133 | Clicking a chip submits it immediately by default, firing `kai-submit` like a typed message — so the streaming loop above just works. To prefill the input instead and let the user edit first, set `suggestionMode = 'fill'` and read the chosen text from `kai-suggestion-click`. For a custom greeting wi |
| `guides/frameworks/html.mdx` | 159 | Wrap panels in `<kai-resizable>` with one `<kai-resizable-item>` each — drag handles are inserted automatically. Each item accepts a `size` (px or `%`) and optional `min`/`max`. Listen for `kai-change` to persist the layout. |
| `components/card.mdx` | 64 | `footer-actions` is responsive on its own: a right-aligned row when there is room, and a full-width vertical stack when the card is too narrow (a container query on the card's width). Author the **primary action last** so it sits at the row's right and rises to the top of the stack. `kai-button` fil |
| `components/file-tree.mdx` | 17 | <p class="kai-lede">Pass a flat array of paths and get a collapsible, keyboard-navigable file explorer — folders are derived from the slashes automatically, no tree to build by hand.</p> |
| `components/prompt-input.mdx` | 27 | Reach for `<kai-prompt-input>` whenever you need a chat composer outside of SolidJS — React, Vue, Svelte, Angular, or plain HTML. It manages its own input state uncontrolled by default; listen for `kai-submit` to receive the typed text and any staged attachments. Pin it to the bottom of a chat surfa |
| `components/reasoning.mdx` | 26 | Place `<kai-reasoning>` between the assistant message content and any tool calls whenever the model produces a chain-of-thought trace. Set `streaming` while the thought arrives and clear it when the stream ends — the element handles expand/collapse automatically. Use `open` for programmatic control, |
| `components/resizable.mdx` | 51 | **Expand-to-fill** — when a `<kai-artifact expandable>` fires `kai-maximize-intent` (bubbling, composed), the nearest enclosing `<kai-resizable>` catches it automatically and hides siblings. No wiring needed. **Escape** or **Collapse** restores the layout. |
| `components/resizable.mdx` | 69 | Wire `<kai-artifact expandable>` inside an item — pressing **Expand** maximizes that panel automatically: |
| `components/workspace.mdx` | 67 | - **Sidebar** — drag-resizable and collapsible. Constrain with `sidebarWidth` (percent, default `22`), `sidebarMinWidth`, and `sidebarMaxWidth`. Collapse is **uncontrolled by default** — the element manages it and fires `kai-sidebar-toggle`. Start collapsed with the `default-sidebar-collapsed` attri |
| `integrations/pydantic-ai.mdx` | 62 | The server binds to `http://localhost:8000` by default. Point `<kai-chat>` at `/api/chat` (or the full URL if it's on a different origin — CORS middleware is already in the route above). |
| `patterns/compose-your-own.mdx` | 101 | - `kai-conversations` accepts a `conversations` property (flat array; the element groups by recency automatically). Listen for `kai-conversation-select` to update `activeId`. |
| `patterns/compose-your-own.mdx` | 103 | - `kai-prompt-input` fires `kai-submit` with `{ value, attachments }`. Set `loading` to `true` while streaming; the send button disables automatically. |
| `patterns/popover-menu.mdx` | 39 | By default the popover manages its own open state. To drive it from your app, set the `open` property and update it from `kai-open-change`: |
| `examples/agentic-assistant.mdx` | 52 | Each assistant message's `parts` array can include `reasoning` and `tool` parts alongside its `text` part. `<kai-chat>` renders every part in order automatically — no extra elements needed. |
| `examples/reasoning-assistant.mdx` | 19 | Reasoning lives in the message's `parts` array, not in a separate element. Add a `{ type: 'reasoning', text, label? }` part ahead of the `{ type: 'text', … }` part; `<kai-chat>` renders it as a collapsible block above the answer. The `label` is the trigger text (defaults to `Reasoning`) and `text` i |

### `packaging` (12) — claims about what the package ships, how it resolves, and what the bundler does with it

| page | line | claim |
|---|---:|---|
| `guides/installation.mdx` | 32 | Import the bundle once for its side effect. This registers every `kai-*` element globally — put it near your app's entry point: |
| `guides/installation.mdx` | 111 | Then import from the root entry (`@kitn.ai/ui`), which ships compiled and tree-shakes to what you use: |
| `guides/installation.mdx` | 131 | \| `@kitn.ai/ui/elements/<element>` \| A single element, tree-shaken. See [Loading](/guides/loading/). \| |
| `guides/installation.mdx` | 134 | \| `@kitn.ai/ui` \| Native SolidJS components — compiled, tree-shakeable; Solid projects only \| |
| `guides/frameworks/overview.mdx` | 20 | Import `@kitn.ai/ui/elements` once for its side effect. It registers every `kai-*` element; after that they work like built-in HTML. |
| `guides/frameworks/overview.mdx` | 141 | Writing Solid? Skip the element wrapper and compose the kit's native components from `@kitn.ai/ui`. They tree-shake to exactly what you import and mix freely with your own primitives. |
| `guides/loading.mdx` | 15 | \| **Per-element** \| `@kitn.ai/ui/elements/chat` \| You use only a couple of elements and your build tree-shakes. \| |
| `guides/loading.mdx` | 19 | `import '@kitn.ai/ui/elements'` registers every element in one import and works everywhere, including SSR. Reach for the other two only when you have a specific reason — a build that tree-shakes, or a no-build CDN page. |
| `guides/loading.mdx` | 28 | One import, every `kai-*` element registered. The simplest setup, and the SSR-safe entry point — the import is side-effect-only and doesn't touch `document` until the browser runs it, so it won't throw in a server render. |
| `guides/loading.mdx` | 114 | \| `@kitn.ai/ui/elements` \| Yes — side-effect only; no `document` on import \| |
| `guides/frameworks/solid.mdx` | 10 | - **Native SolidJS components** (`@kitn.ai/ui`) — import `ChatContainer`, `Message`, `PromptInput`, and the rest directly into your JSX. Full compositional control, tree-shaken alongside your own code. Reach for this in a Solid app. |
| `guides/frameworks/solid.mdx` | 32 | The `@kitn.ai/ui` entry point (`"."`) exports raw TSX. Your bundler compiles and tree-shakes it alongside your own Solid code — you only pay for what you import. |

### `runtime` (11) — claims about SSR, server rendering, hydration and where code runs

| page | line | claim |
|---|---:|---|
| `guides/loading.mdx` | 14 | \| **Register all** \| `@kitn.ai/ui/elements` \| The default. You use several elements, or you just want it to work. Works in SSR. \| |
| `guides/loading.mdx` | 99 | The autoloader resolves element modules relative to its own URL and uses `document` + `MutationObserver`. Load it from a `<script type="module">` on a CDN or static host. Do **not** `import '@kitn.ai/ui/autoloader'` in a bundled (Vite/webpack/Next) app or a server render — the bundler relocates it a |
| `guides/loading.mdx` | 115 | \| Per-element (`@kitn.ai/ui/elements/chat`) \| No — use inside a client-only boundary \| |
| `guides/frameworks/meta-frameworks.mdx` | 8 | <p class="kai-lede">AI/UI elements are custom elements with Shadow DOM, so they hydrate and run in the browser. In Next.js and TanStack Start that makes them client islands: the page renders on the server, and `kai-chat` comes alive where it mounts.</p> |
| `guides/frameworks/meta-frameworks.mdx` | 82 | Same idea, different gates. Wrap the chat in `ClientOnly` from `@tanstack/react-router` so it mounts only after hydration, and register the elements in an effect: |
| `components/conversations.mdx` | 65 | - **Declarative children** — nest `<kai-conversation>` tags directly; each carries its `id` as an attribute and title as text content. The element reads them on mount and watches via MutationObserver — works in plain HTML, SSR, or any framework that sets innerHTML. |
| `components/skills.mdx` | 25 | Place `<kai-skills>` beneath an assistant message row to annotate which tools were active. Use the `skills` property when skill data comes from app state; use declarative `<kai-skill>` children for server-rendered HTML with no JS wiring. |
| `components/skills.mdx` | 43 | - **Declarative path** — nest `<kai-skill id="…">Label</kai-skill>` children directly; useful for server-rendered HTML with no JS wiring. |
| `components/sources.mdx` | 25 | Place `<kai-sources>` directly below an assistant message when the reply cites external references. Use `numbered` when the answer body uses `[1]`, `[2]`, … citation markers. Use the `sources` property for dynamic or streamed lists; use declarative `<kai-source>` children for static or server-render |
| `integrations/connect-any-backend.mdx` | 71 | `<kai-chat>` is a web component — it hydrates and runs in the browser. That holds in a plain HTML page, a React app, or a Next.js route (where it's a client island). The component is the front end; your `/api/chat` route is the back end. Server rendering emits the tag; the chat comes alive on hydrat |
| `theme/editor.mdx` | 9 | {/* Server-rendered marker: drives the full-screen CSS in app.css (body:has(.kai-fullscreen)) |

### `size-perf` (3) — claims about bundle size, code splitting and lazy loading

| page | line | claim |
|---|---:|---|
| `guides/installation.mdx` | 132 | \| `@kitn.ai/ui/autoloader` \| CDN / static auto-loader — loads each `kai-*` on demand, no build. See [Loading](/guides/loading/). \| |
| `components/resizable-item.mdx` | 25 | Use `<kai-resizable-item>` as a direct child of `<kai-resizable>` to define each panel. Give panels with a natural default width an explicit `size` (e.g. a sidebar at `"260px"`); leave `size` off the main content area so it fills remaining space. Use `locked` for chrome panels that must never resize |
| `patterns/artifact-from-message.mdx` | 8 | <p class="kai-lede">When the assistant generates a runnable result — a web page, an app, a component — your backend serves it at a URL and `<kai-artifact>` frames it live. You don't have to show it the moment it's ready: keep the conversation full-width, reference the generated file in the message, |

### `version` (3) — claims tied to a version number

| page | line | claim |
|---|---:|---|
| `guides/getting-started.mdx` | 35 | Pin an exact version in production (e.g. `@kitn.ai/ui@0.16.0/dist/...`). The package is pre-1.0, so a minor release can break things. |
| `guides/installation.mdx` | 100 | An unpinned CDN URL tracks the latest release — fine for prototyping, risky for production. The package is pre-1.0, so a minor release can break things. Pin an exact version: `https://cdn.jsdelivr.net/npm/@kitn.ai/ui@0.16.0/dist/kai.es.js`. |
| `guides/loading.mdx` | 103 | An unversioned CDN URL tracks the latest release, which is risky on a pre-1.0 package. Pin an exact version: `https://cdn.jsdelivr.net/npm/@kitn.ai/ui@0.16.0/dist/elements/autoloader.js`. |

## Honest assessment: how much of the drift is mechanically detectable

Roughly **a third**, and it is the cheap third.

What a machine settles conclusively: does this name exist, does this prop exist, does this element exist, does this event exist, does this snippet compile against the shipped types, is this local type compatible with the kit's. Every finding above is one of those, and each is either true or false with no judgement involved. That is the whole first two sections of this report.

What it cannot settle, and where the rest of the drift lives:

- **Whether a snippet that compiles actually works.** Nothing here is executed. The parts-flattening bug in `guides/frameworks/solid.mdx` compiled cleanly for as long as it existed, and was only caught because the page happened to re-declare a type the kit exports. A page that flattens `parts` without re-declaring anything would still be invisible.
- **Whether prose is true.** Names are checked; claims are not. The 47 items flagged for human review are the ones worth checking, and the harness deliberately refuses to guess at any of them.
- **Whether the recommended approach is still the recommended approach.** A page can be entirely correct and entirely outdated.
- **Whether an example is good.** Ordering, completeness, error handling, whether the snippet teaches the pattern the kit wants taught.

The honest framing: this harness makes the mechanical third *free and repeatable*, and it makes the other two thirds **visible and bounded** — 47 extracted claims and two reverse-coverage lists are a finite human task, where "read 120 pages and check everything" is not. It is a filter, not a verdict.

One more caveat worth stating plainly: this harness has already been wrong. Its first full run reported 73 high-confidence findings; 64 of those were its own bugs (Vue's `.prop` modifier, Svelte's `{messages}` shorthand, a CSS custom property parsed as an element, prose inside comments, the docs' own counter-examples). Each was found by checking a sample of findings against the source by hand. The number below is what survived that; it is not proof that nothing else is wrong with it.

## What this harness does NOT cover

Stated plainly, because a coverage claim nobody bounded is how a check ends up proving nothing.

- **Prose meaning.** Only names are checked, not sentences. "`ChatContainer` owns the scroll" is unverifiable here; the harness only knows `ChatContainer` exists.
- **Runtime behaviour.** No snippet is executed and no page is rendered. A snippet that compiles and renders nothing looks identical to a correct one.
- **Visual/UX claims.** Screenshots, layout, theming results.
- **Untyped JS depth.** `js` and inline `<script>` blocks are checked for kit-API drift only. A logic bug in an untyped snippet is invisible.
- **Non-code fences.** 26 block(s) in languages with no checker here (bash, json, python, sh, toml).
- **Uninstalled third-party packages.** 15 specifier(s) resolve to `any` by design, so misuse of them is not caught: `@ai-sdk/openai`, `@angular/core`, `@angular/platform-browser`, `@langchain/core/tools`, `@langchain/langgraph/prebuilt`, `@langchain/openai`, `@mastra/client-js`, `@tanstack/react-router`, `@vitejs/plugin-vue`, `ai`, `https://cdn.jsdelivr.net/npm/@kitn.ai/ui/dist/elements/autoloader.js`, `https://cdn.jsdelivr.net/npm/@kitn.ai/ui/dist/kai.es.js`, `openai`, `vite`, `zod`.
- **Angular / Vue / Svelte templates.** Only the `<script>` half is compiled; the template half gets the same structural `kai-*` checks as HTML.

## Reproducing

```bash
pnpm exec nx build ui                       # the harness reads dist/*.d.ts
pnpm --filter @kitn.ai/docs run verify:docs # gate: exits 1 on any high finding
pnpm --filter @kitn.ai/docs run verify:docs:report  # regenerate this file
```

The self-test runs first, every time. It compiles five snippets that MUST produce a kit finding (unknown entry point, unknown export, bogus prop on a kit component, wrong argument type, removed wire API), seven structural probes that MUST report (unknown element, unknown prop, non-scalar-as-attribute, unknown event, unknown part, stale `<Example tag>`, stale `<Example config>` key), a shadow probe that must go from clean to broken when the real type is substituted, and five probes that MUST stay silent (reader-owned undeclared names, omitted imports, uninstalled third-party imports, valid markup, non-scalar props bound as JSX expressions). If any probe misbehaves the run aborts before touching the docs, because a green result would be meaningless.
