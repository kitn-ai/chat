diff --git a/packages/create-kai/scripts/build.mjs b/packages/create-kai/scripts/build.mjs
index 94a2e8aa..30e66bfe 100644
--- a/packages/create-kai/scripts/build.mjs
+++ b/packages/create-kai/scripts/build.mjs
@@ -155,7 +155,7 @@ async function main() {
   await rm(dist, { recursive: true, force: true });
   await mkdir(templatesOut, { recursive: true });
 
-  // The block gallery rides the CLI the way the templates do: a plain copy of
+  // The block catalog rides the CLI the way the templates do: a plain copy of
   // the kit's authored blocks into dist/, which `add` walks at runtime. The
   // rules over its contents live where a test can drive them - the registry
   // module validates every manifest and `test/add.test.ts` drives every block
diff --git a/packages/create-kai/src/blocks.ts b/packages/create-kai/src/blocks.ts
index ba2048d2..4e1e7f2c 100644
--- a/packages/create-kai/src/blocks.ts
+++ b/packages/create-kai/src/blocks.ts
@@ -36,8 +36,8 @@ import type {
 } from '@kitn.ai/blocks';
 // The FORM RENDERING is the kit's shared pure module too (same bundle-import
 // precedent as the registry line above): one renderer serves this planner AND
-// the `kai dev` gallery's per-framework code view, so what the gallery shows
-// is byte-for-byte what `add` writes.
+// the per-framework code view on the docs site's /blocks section, so what
+// /blocks shows is byte-for-byte what `add` writes.
 import {
   adaptRegistrationForBundler,
   componentName,
@@ -320,7 +320,7 @@ export function planAdd(resolved: ResolvedAdd, opts: PlanOptions): AddPlan {
 }
 
 // The three per-form file sets come from the ONE shared renderer
-// (`@kitn.ai/blocks/forms` — the gallery serves the identical output);
+// (`@kitn.ai/blocks/forms`, which is what /blocks shows too);
 // what stays here is what only the CLI knows: where the files land in the
 // consumer's project, and the note printed about them.
 
diff --git a/packages/create-kai/src/build-guards.ts b/packages/create-kai/src/build-guards.ts
index d87588e2..c7068be6 100644
--- a/packages/create-kai/src/build-guards.ts
+++ b/packages/create-kai/src/build-guards.ts
@@ -417,8 +417,8 @@ export function missingStarterProblem(
  * is not there.
  *
  * `@kitn.ai/blocks` lives in its own workspace package (see packages/blocks):
- * one source for the block registry and forms shared by the `kai dev`
- * gallery and this CLI, behind a package boundary instead of a relative
+ * one source for the block registry and forms shared by the docs site's
+ * /blocks section and this CLI, behind a package boundary instead of a relative
  * reach across packages. `cp` throws its own ENOENT on a missing source, so this
  * rule buys the same thing missingStarterProblem buys for a starter: the
  * message names the resolved directory instead of leaving a stack trace to
diff --git a/packages/create-kai/src/react-form.ts b/packages/create-kai/src/react-form.ts
index 411ff64b..a0def861 100644
--- a/packages/create-kai/src/react-form.ts
+++ b/packages/create-kai/src/react-form.ts
@@ -1,6 +1,6 @@
 /**
- * The block form renderers moved to the kit's shared pure module layer —
- * `@kitn.ai/blocks/forms` — so the `kai dev` gallery
+ * The block form renderers moved to the kit's shared pure module layer,
+ * `@kitn.ai/blocks/forms`, so the docs site's /blocks section
  * and this CLI render every delivery form through ONE renderer (the same
  * precedent as `registry.ts`, which `blocks.ts` already bundle-imports; the
  * rationale comments travel with the functions). This shim keeps the CLI's
