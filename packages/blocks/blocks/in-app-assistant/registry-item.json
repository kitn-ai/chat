{
  "name": "in-app-assistant",
  "title": "In-app assistant",
  "description": "An assistant docked inside your existing app: a persistent side panel with kai-panel chrome, kai-view-stack drill into conversation history, composer slash and mention triggers, and the headless conversation controller over local storage. Ships with a scripted mock so the thread demos reasoning and a settled search_docs call on first paint.",
  "type": "registry:block",
  "categories": ["assistant", "aside"],
  "files": [
    { "path": "in-app-assistant.html", "type": "registry:page" },
    { "path": "in-app-assistant.controller.ts", "type": "registry:file" },
    { "path": "in-app-assistant.css", "type": "registry:file" },
    { "path": "mock.ts", "type": "registry:file" }
  ],
  "dependencies": ["@kitn.ai/ui"],
  "registryDependencies": [],
  "cssVars": { "light": {}, "dark": {} },
  "envVars": {},
  "docs": "Runs against a scripted local mock out of the box - no endpoint, no key. The host page content is a stand-in: keep the aside, replace the main region with your app. To go live, replace the mock responder in in-app-assistant.controller.ts with a fetch to your chat endpoint and keep parsing through the @kitn.ai/ui/wire readers.",
  "meta": { "iframeHeight": "720px" }
}
